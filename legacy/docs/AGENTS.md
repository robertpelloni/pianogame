# AI Agent Universal Instructions

For this specific project (PianoGame), prioritize reading `src/version.h` to align version numbers.
Ensure any changes are tested across the different environments if possible, though C++ environments may require specific compilation steps.
There is no UNIVERSAL_LLM_INSTRUCTIONS document.
