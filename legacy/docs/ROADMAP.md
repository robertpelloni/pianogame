# Roadmap

- Better configuration and settings management (replacing hard-coded configuration).
- Expanded cross-platform support.
- Refactoring the OpenGL and OS interaction layers to make it easier to add new UI elements.
