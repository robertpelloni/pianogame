# DEPLOY

## Current Deployment Instructions
Currently transitioning. Legacy C++ builds are configured via:
* macOS: `Synthesia.xcodeproj`
* Windows: `PianoGame.sln` / `PianoGame.vcproj`

## Future Deployment Environment Setup
This document will be updated as the Rust, Go, C#, Java, and TypeScript architectures are scaffolded.
* Each language implementation will require its own detailed environment setup instructions.
* MCP Server setup and Browser Extension sideloading guides will be added here.
* Build automation and continuous integration pipelines will be strictly documented.
